# Python
__pycache__/
*.py[cod]
*.pyo
*.egg-info/
.eggs/
dist/
build/
*.egg
.venv/
venv/
env/
.env

# Testing
.pytest_cache/
.coverage
htmlcov/
.mypy_cache/
.ruff_cache/

# Node / Frontend
node_modules/
apps/dashboard/dist/
apps/dashboard/.vite/

# Raw portal data (may contain PII — ticket holder names, phone numbers)
# Adjust this if you want to commit raw artifacts for debugging
data/raw/
data/parsed/
data/tn811-monitor.log

# Database (contains PII — keep out of version control in production)
# For development you may want to track this; comment out if so
data/tn811.db

# Keep sample exports (no PII beyond ticket numbers, needed for dashboard dev)
!data/exports/

# Playwright
playwright-report/

# OS
.DS_Store
Thumbs.db

# Editor
.vscode/
.idea/
*.swp
*.swo

# Secrets (never commit these)
.env
*.pem
*.key
